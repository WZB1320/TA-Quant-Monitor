# QuantMonitor — 量化交易监控系统

一套面向量化投资交易员的 **A 股多因子策略回测平台**，支持自选股分组管理、分组级策略参数配置、历史回测与绩效可视化。

## 功能概览

| 模块 | 说明 |
|------|------|
| **自选股管理** | 分组管理自选股（科技成长型 / 消费稳健型 / 周期资源型 …），支持搜索添加、换组、删除 |
| **策略参数配置** | 按分组独立配置指标参数、指标权重、体制权重、信号引擎参数，互不干扰 |
| **回测分析** | 选定时间区间与分组运行回测，展示净值曲线、回撤曲线、6 项核心绩效指标、交易明细 |

## 技术架构

```
┌─────────────────────────────────────────────────────┐
│  前端  (React + Vite + TypeScript + Ant Design)      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ 自选股   │ │ 策略配置 │ │ 回测分析 │            │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘            │
│       └─────────────┼────────────┘                  │
│              Axios / REST 接口                       │
└─────────────────────┬───────────────────────────────┘
                      │
┌─────────────────────┼───────────────────────────────┐
│  后端  (FastAPI + Uvicorn)                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ 自选股   │ │ 策略配置 │ │ 回测分析 │            │
│  │ 增删改查 │ │  接口    │ │  接口    │            │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘            │
│       └─────────────┼────────────┘                  │
│              JSON 文件存储                           │
└─────────────────────┬───────────────────────────────┘
                      │
┌─────────────────────┼───────────────────────────────┐
│  核心引擎  (Python)                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ 数据获取 │ │ 指标计算 │ │ 信号引擎 │            │
│  │ AkShare  │ │ 计算管线 │ │ 评分器   │            │
│  └──────────┘ └──────────┘ └──────────┘            │
│  ┌──────────┐ ┌──────────┐                          │
│  │ 回测引擎 │ │ 仓位管理 │                          │
│  └──────────┘ └──────────┘                          │
└─────────────────────────────────────────────────────┘
```

## 目录结构

```
.
├── config/
│   └── strategy_config.json    # 策略参数配置（分组级）
├── data/
│   ├── watchlist.json          # 自选股列表
│   ├── kline_cache.db          # K线数据缓存 (SQLite)
│   └── signal_history.json     # 信号去重历史
├── src/                        # 核心引擎 (Python)
│   ├── backtest/               # 回测引擎
│   │   ├── engine.py           #   回测主循环
│   │   ├── broker.py           #   模拟撮合
│   │   ├── position.py         #   仓位管理
│   │   ├── metrics.py          #   绩效指标
│   │   └── report.py           #   报告生成
│   ├── config/                 # 配置管理
│   │   ├── settings.py         #   全局设置
│   │   └── group_config.py     #   分组配置加载
│   ├── data_fetcher/           # 数据获取
│   │   ├── akshare_source.py   #   AkShare 数据源
│   │   ├── baostock_source.py  #   BaoStock 数据源
│   │   ├── cache.py            #   K线缓存
│   │   ├── manager.py          #   数据管理器
│   │   └── watchlist.py        #   自选股加载
│   ├── indicators/             # 技术指标
│   │   ├── trend.py            #   趋势指标 (MA60, EMA, MACD)
│   │   ├── strength.py         #   强度指标 (ADX, ATR)
│   │   ├── momentum.py         #   动量指标 (RSI, KDJ)
│   │   ├── volume.py           #   量价指标 (OBV, VOL_RATIO)
│   │   └── pipeline.py         #   指标计算管线
│   └── signal_engine/          # 信号引擎
│       ├── engine.py           #   信号生成主逻辑
│       ├── scorer.py           #   多因子评分
│       ├── filter.py           #   信号过滤
│       └── validator.py        #   信号校验
├── web/
│   ├── backend/                # Web 后端
│   │   ├── app.py              #   FastAPI 入口
│   │   ├── schemas.py          #   Pydantic 模型
│   │   └── routers/
│   │       └── watchlist.py    #   自选股 增删改查 路由
│   └── frontend/               # Web 前端
│       └── src/
│           ├── api/index.ts    #   Axios 接口封装
│           ├── components/     #   公共组件
│           ├── pages/          #   页面组件
│           │   ├── Watchlist/  #     自选股管理
│           │   ├── Config/    #     策略配置
│           │   └── Backtest/  #     回测分析
│           └── stores/         #   Zustand 状态管理
├── scripts/                    # 辅助脚本
│   ├── current_signals.py      #   查看当前信号
│   ├── analyze_trades.py       #   交易分析
│   └── plot_equity.py          #   净值曲线绘制
├── run_backtest.py             # 一键回测入口
└── tests/                      # 单元测试
```

## 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+
- npm 9+

### 1. 安装 Python 依赖

```bash
pip install fastapi uvicorn akshare baostock pandas numpy
```

### 2. 启动后端

```bash
cd web/backend
uvicorn app:app --reload --port 8000
```

后端接口文档: http://localhost:8000/docs

### 3. 安装前端依赖并启动

```bash
cd web/frontend
npm install
npm run dev
```

前端页面: http://localhost:5173

### 4. 命令行回测 (无需前端)

```bash
python run_backtest.py
```

## 策略体系

### 多因子评分模型

信号引擎对每只股票计算综合得分，由 **4 大类 8 个指标** 加权汇总：

| 类别 | 指标 | 作用 |
|------|------|------|
| 趋势 | MA60, EMA 双线, MACD | 判断多空方向与趋势强度 |
| 强度 | ADX, ATR | 识别市场体制（趋势/震荡）与波动率 |
| 动量 | RSI, KDJ | 捕捉超买超卖与拐点信号 |
| 量价 | OBV, 量比 | 验证量价共振与资金流向 |

### 体制自适应权重

系统根据 ADX 自动识别当前市场体制，动态切换指标权重：

- **趋势市** (ADX > 25): 趋势类权重提升至 40%
- **震荡市** (ADX < 20): 动量类权重提升至 50%
- **过渡期** (20 < ADX < 25): 均衡配置

### 分组差异化策略

每个自选股分组拥有独立的策略参数：

| 分组 | 特点 | 得分阈值 | ATR止损 | RSI周期 | 核心差异 |
|------|------|----------|---------|---------|----------|
| 科技成长型 | 高Beta、题材驱动 | 45 | 2.0× | 9 | RSI短周期+OBV高权重 |
| 消费稳健型 | 慢牛低波动 | 30 | 1.8× | 21 | MA60高权重+MACD标准 |
| 周期资源型 | 大宗驱动、周期强 | 25 | 2.5× | 21 | ADX高权重+量价并重 |
| 医药创新型 | 事件驱动、均值回归 | 40 | 2.5× | 21 | RSI/KDJ高权重+MACD降权 |
| 机械制造型 | 趋势与量价并重 | 40 | 2.5× | 21 | ADX+VOL_RATIO强化 |

### 仓位与风控

- **ATR 动态止损**: 入场价 - N×ATR，N 按分组配置（1.8 ~ 2.5）
- **移动止盈**: 盈利 10% 以下 2.5×ATR → 10~20% 2.0×ATR → 20% 以上 1.5×ATR
- **连亏保护**: 连续亏损 N 笔后暂停交易 M 天（按分组配置）
- **冷却期**: 卖出后 N 天内不再买入同一标的

## 自定义配置

### 添加自选股

编辑 `config/strategy_config.json` 中的 `watchlist` 字段，或通过前端页面操作：

```json
"科技成长型": [
  { "code": "000725", "name": "京东方A", "market": "sz" }
]
```

### 调整策略参数

在 `group_config.groups` 中修改对应分组的参数，或通过前端策略配置页面调整：

```json
"科技成长型": {
  "score_threshold": 45,
  "atr_stop_mult": 2.0,
  "indicator_weights": {
    "RSI": 0.22,
    "OBV": 0.20
  }
}
```

### 新增分组

1. 在 `watchlist` 中添加新分组及股票
2. 在 `group_config.groups` 中添加对应策略参数
3. 未配置的分组自动使用 `_default` 参数

## 数据源

- **AkShare**: A 股实时行情、股票搜索
- **BaoStock**: 历史日 K 线数据
- **本地缓存**: SQLite 缓存 K 线数据，减少重复请求

## 开源协议

MIT

## 作者声明

本项目为个人学习研究作品，**个人用户免费使用**。禁止任何形式的商业销售、转售或用于盈利性服务。欢迎 Star 与 Fork，转载请注明出处。
